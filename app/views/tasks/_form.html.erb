<%= form_for @task do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>
  <%= form.label :description %>
  <%= form.text_field :description %>
  <%= form.check_box :done if @task.persisted? %>
  <%= form.submit %>
<% end %>
<%= link_to "Cancel", tasks_path %>
